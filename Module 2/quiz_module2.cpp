#include <iostream>

using namespace::std;

int main(){

    int nb_etudiants(300);
    int nb_places(600);
    if(nb_etudiants * 2 !<  nb_places){ // fausse
        cout<<"Ca ne marche pas"<<endl;
    }
    /* code */
    return 0;
}
